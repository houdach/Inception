docker compose -f srcs/docker-compose.yml down -v
docker compose -f srcs/docker-compose.yml build mariadb
docker compose -f srcs/docker-compose.yml up -d mariadb
docker compose -f srcs/docker-compose.yml build wordpress
docker compose -f srcs/docker-compose.yml up -d wordpress
docker ps
docker logs wordpress
RUN apt update && apt install -y php-fpm php-mysql php-cli php-mbstring php-xml php-gd unzip curl \
    && php-fpm -v \
    && rm -rf /var/lib/apt/lists/*
nginx -g -daemon off
 docker exec nginx openssl s_client -connect localhost:443 -tls1_3